<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>#(title) | Notes</title>
</head>
<body>
<h1>Hello CodeMash!</h1>
        #if(notes) {
            <table>
                <thead>
                    <tr>
                        <th>Session title></th>
                        <th>Presenter</th>
                    </tr>
                </thead>
                <tbody>
                    #for(note in notes) {
                        <tr>
                            <td>#(note.title)</td>
                            <td>#(note.presenter)</td>
                        <tr>
                    }
                </tbody>
            </table>
            } else {
                <h2>You don't have any notes yet!</h2>
            }
</body>
</html>
